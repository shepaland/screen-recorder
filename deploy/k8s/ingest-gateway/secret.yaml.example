apiVersion: v1
kind: Secret
metadata:
  name: ingest-gateway-secret
  labels:
    app: ingest-gateway
type: Opaque
data:
  # base64-encoded values — REPLACE with real values before deploying
  # echo -n 'your-value' | base64
  DB_USERNAME: CHANGE_ME_BASE64
  DB_PASSWORD: CHANGE_ME_BASE64
  INTERNAL_API_KEY: CHANGE_ME_BASE64
  S3_ACCESS_KEY: CHANGE_ME_BASE64
  S3_SECRET_KEY: CHANGE_ME_BASE64
