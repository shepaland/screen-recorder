apiVersion: v1
kind: Service
metadata:
  name: ingest-gateway
  labels:
    app: ingest-gateway
spec:
  type: ClusterIP
  selector:
    app: ingest-gateway
  ports:
    - port: 8084
      targetPort: 8084
      protocol: TCP
