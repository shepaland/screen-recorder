# This is a TEMPLATE. Per-environment ingress is in deploy/k8s/envs/<env>/ingress.yaml
# See deploy/k8s/envs/prod/ingress.yaml and deploy/k8s/envs/test/ingress.yaml
#
# Both environments use path-based routing: /screenrecorder → web-dashboard
# Traefik StripPrefix middleware removes /screenrecorder before forwarding to nginx
