apiVersion: v1
kind: Service
metadata:
  name: web-dashboard
  labels:
    app: web-dashboard
spec:
  type: ClusterIP
  selector:
    app: web-dashboard
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
